__pycache__/
*.pyc
runs/
dist/
build/
*.egg-info/
*.html
*.jsonl
_scratch.txt
.lastrun
.orientim-licence
*signing_key*
tests/_runs/
.orientim/
.pytest_cache/
orientim-report.json
lab/_runs/
# `python examples/regression.py` deletes and rewrites this whole
# directory on every run, so nothing in it is worth tracking. Three of
# its files used to be, and a clean clone got case files pointing at
# recordings the clone did not have — *.jsonl is ignored, so those
# recordings were never in git at all.
examples/_regression/
.coverage
